class SQLParser:
    @staticmethod
    def format_commands(sql_commands):
        formatted_queries = []
        for command in sql_commands:
            formatted_queries.append(command)
        return formatted_queries
